# Taktchat — continuidade de sessão

**Projeto:** Taktchat  
**Repositório:** https://github.com/zanon-alive/taktchat.git  
**Branch atual:** `docs/kit-documentacao-produto`

## Estado

Kit **v1.6**. Caminho A da demanda 16 encerrado nesta linha (A1–A9 + desfecho ao Encerrar + tags `#`). Checklist de navegação atualizado com a validação de UI. **Pusheado** em `origin/docs/kit-documentacao-produto` (`9eff7f9`), sem PR.

Este repositório **não** é projeto Telecontrol.

## Ainda aberto (não é código desta branch)

1. Captura **real** do celular (print ainda é ilustração de IA).
2. Seed **não** vira migration / não vai para a VPS.
3. Caminho B (Deal / CRM de mercado).
4. PR — o usuário decide depois do push. Deploy — nunca automático.
5. Local: ticket #15 (Mercado Central / Suporte) veio de um teste meu; o usuário pediu para não mexer. Branches `feat/*` mergeadas seguem existindo por escolha dele.

## Próximo passo

Aguardar o usuário decidir sobre o PR de `docs/kit-documentacao-produto` → `main`.
