# Taktchat — continuidade de sessão

**Branch:** `feat/landing-conversao`

## Estado
- Demanda validada pelo usuário. Commit e PR desta sessão.
- Revisão jurídica de `/lgpd` continua em `.docs/pendencias/termos-privacidade-lgpd.md`.

## Entrega
- `/landing` v2, `/landing/v1` arquivo noindex, `/lgpd` genérica.
- Visitante em `/` → `/landing`; logado → dashboard.
