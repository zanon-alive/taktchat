# Manual — Supervisor operacional

Não existe `profile = supervisor`. É um **atendente com flags**: dashboard, tempo real, `allTicket=enable`.

Login de teste: `supervisor@taktchat.local`.

Permissões extras vs. Beatriz: `tickets.update`, `tickets.transfer`, `dashboard.view`, `reports.view`, `realtime.view`.

## O que você faz

- Ver tickets de outros (conforme `allTicket`) — na UI, Diego vê Ana (dele) e João pending em Vendas
- Dashboard (`/` — print `f26`) e menu **Painel** (`/moments`)
- Transferir e acompanhar fila

Use o [manual do atendente](atendente.md) para o chat. Use o [admin](admin-empresa.md) só se também for admin.
