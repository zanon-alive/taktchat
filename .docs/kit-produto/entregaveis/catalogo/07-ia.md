# IA

## Para que serve

Prompts e assistentes com OpenAI/Gemini, RAG (arquivos da empresa) e transcrição de áudio.

## Onde fica

`/prompts`, `/ai-settings`, `/files`

## Dependências

Chave do provedor nas settings da empresa e plano com `useOpenAi`. RAG exige PostgreSQL com extensão `pgvector`; jobs de indexação podem depender de Redis. Nesta máquina a chave do Cliente Demo está vazia.

## Status

**Condicional:** serviços implementados, mas não exercitados sem chave/`pgvector`. Não vender como “já funciona no demo local”.
**Sem rota:** `SmartFilesDashboard` existe como página, mas não é tela acessível na navegação atual.
